(function(window, $){

    var templatesConfig = {
        deck: 'templates/deck.html'
    };

    window.addToNamespace('GTDapp.config', {
        templates: templatesConfig
    });

}) (window, jQuery);